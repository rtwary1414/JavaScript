"use strict"; // treat all JS code as newer version

// alert(3 + 3) // we are using nodejs not browser

console.log(3 + 3)
console.log("Hitesh")

// Code Readability should be high

// DataTypes: 

let name = "Ratnesh";
let age = 25;
let isLoggedIn = false;

// number => 2 to the power 53
// bigint
// string => can use single and double quotes both
// boolean

// null => This is a standalone value. It is a representation of an empty value. It states that a variable is not having a value.
// undefined => when a variable is not defined then this value is stored. It states that a variable has not been assigned a value.

//symbol => Whenever we talk about uniqueness, we use symbol. 

// object 

console.log(typeof null)
console.log(typeof undefined)
console.log(typeof age)